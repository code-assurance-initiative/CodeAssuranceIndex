<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <AssemblyName>Cai.Scoring</AssemblyName>
    <RootNamespace>Cai.Scoring</RootNamespace>
  </PropertyGroup>

  <!-- NuGet package: the open CAI reference scorer, so any analyzer (Watchdog included) folds an evidence bundle
       through the SAME implementation rather than re-deriving the math. NOT PUBLISHED YET: publish-packages.yml has
       never run and neither package exists on nuget.org or GitHub Packages, so "read our algorithm and check our
       number" is not yet true for a stranger. nuget.org is the target because GitHub Packages needs an account even
       for a public feed; it needs the NUGET_API_KEY secret on the repository, which is not set. -->
  <PropertyGroup>
    <IsPackable>true</IsPackable>
    <PackageId>Cai.Scoring</PackageId>
    <!-- Keep in lockstep with Cai.Delivery: the two ship as one reference implementation of the same rubric
         generation, and a consumer that mixes versions can recompute a different number. -->
    <Version>0.2.0</Version>
    <Authors>The Code Assurance Initiative</Authors>
    <Copyright>Copyright 2026 The Code Assurance Initiative</Copyright>
    <Company>The Code Assurance Initiative</Company>
    <Description>The CAI (Code Assurance Index) open reference scorer: folds an evidence bundle (per-dimension scores + meta-dimensions + surface metrics) through the rubric — confidence-weighted categories, worst-first OWA lenses, the architecture surface floor, quality-bar bands, critical gating and the coherence cap — into the 0–100 headline and ten lens scores. Deterministic and reproducible: same evidence + same rubric ⇒ the same number.</Description>
    <PackageProjectUrl>https://codeassuranceindex.info</PackageProjectUrl>
    <RepositoryUrl>https://github.com/code-assurance-initiative/CodeAssuranceIndex</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <!-- MUST match the repository LICENSE (Apache-2.0). A standard whose reference implementation carries
         contradictory licence metadata is not credibly open. -->
    <PackageLicenseExpression>Apache-2.0</PackageLicenseExpression>
    <PackageTags>cai;code-assurance-index;scoring;code-quality;rubric;owa</PackageTags>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <PublishRepositoryUrl>true</PublishRepositoryUrl>
    <EmbedUntrackedSources>true</EmbedUntrackedSources>
    <IncludeSymbols>true</IncludeSymbols>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <Deterministic>true</Deterministic>
  </PropertyGroup>

  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="\" />
    <!-- Ship the licence text inside the package, not just as an SPDX expression, so an offline consumer
         auditing the reference implementation has the terms in hand. -->
    <None Include="..\..\LICENSE" Pack="true" PackagePath="\" />
    <!-- NOTICE travels with LICENSE: Apache-2.0 §4(d) requires it to be carried with any
         distribution, and it is where this package says who holds the copyright. -->
    <None Include="..\..\NOTICE" Pack="true" PackagePath="\" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.SourceLink.GitHub" Version="10.0.401" PrivateAssets="All" />
  </ItemGroup>
</Project>
